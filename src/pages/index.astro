---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
---

<Layout title="Glitch | Redefiniendo el error">
	<Header />

	<!-- Sección Hero -->
	<header
		class="min-h-screen flex flex-col justify-center items-center text-center px-6 relative pt-20"
	>
		<div
			class="absolute inset-0 overflow-hidden pointer-events-none opacity-20"
		>
			<div
				class="text-[20vw] font-bold text-white/5 absolute -bottom-10 -left-10 select-none heading-font"
			>
				ERROR
			</div>
		</div>

		<h1
			class="heading-font text-5xl md:text-8xl font-black mb-6 leading-none glitch"
			data-text="GRUPO GLITCH"
		>
			GRUPO GLITCH
		</h1>
		<p
			class="max-w-2xl text-lg md:text-xl text-gray-400 mb-10 tracking-tight"
		>
			No somos una empresa. Somos jóvenes intentando hacer el Glitch en
			esta sociedad. <span
				class="text-white italic underline decoration-cyan-500"
				>Hackeamos el futuro.</span
			>
		</p>

		<div class="flex flex-wrap justify-center gap-6">
			<a
				href="#services"
				class="btn-glitch uppercase tracking-widest text-sm font-bold"
				>Protocolos</a
			>
			<a
				href="#contact"
				class="px-6 py-3 border border-white/20 hover:border-white transition-all uppercase tracking-widest text-sm"
				>Contactar
			</a>
		</div>

		<div class="mt-20 animate-bounce opacity-50">
			<svg
				xmlns="http://www.w3.org/2000/svg"
				class="h-6 w-6 mx-auto"
				fill="none"
				viewBox="0 0 24 24"
				stroke="currentColor"
			>
				<path
					stroke-linecap="round"
					stroke-linejoin="round"
					stroke-width="2"
					d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
			</svg>
		</div>
	</header>

	<!-- Sección About Us -->
	<section id="about" class="py-24 px-6 bg-white/5 border-y border-white/5">
		<div class="">
			<div>
				<h2
					class="heading-font text-3xl mb-8 border-l-4 border-cyan-500 pl-6"
				>
					Nuestra Filosofía
				</h2>
				<div class="space-y-6 text-gray-300">
					<p>
						La organización <strong>GLITCH</strong> nace de la mala programación
						de un sistema.
						<br />
						<br />
						Creemos que la perfección es estática, mientras que el error
						es progreso.
					</p>
					<p>
						Damos solucion a problemas modernos, utilizando el ruido
						digital como nuestra principal herramienta.
					</p>
				</div>
			</div>
		</div>
	</section>

	<!-- Sección de Servicios -->
	<section id="services" class="py-44 px-6">
		<div class="max-w-6xl mx-auto">
			<div class="text-center mb-20">
				<h2
					class="heading-font text-4xl mb-4 glitch"
					data-text="EQUIPOS"
				>
					Equipos
				</h2>
				<p class="text-gray-500">Nuestras líneas de interferencia</p>
			</div>

			<div class="grid md:grid-cols-3 gap-8">
				<!-- Tarjeta 1 -->
				<a
					href="code/index.html"
					class="card-glitch border border-white/10 p-8 bg-black/50 backdrop-blur-sm relative overflow-hidden group block"
				>
					<div class="text-cyan-500 mb-6 heading-font text-xl">
						01 // Dev Team
					</div>
					<h3 class="text-2xl font-bold mb-4">
						Desarrollo de Software
					</h3>
					<p class="text-gray-400 text-sm leading-relaxed mb-6">
						Desarrollo de software con código limpio y alma rebelde.
					</p>
					<div
						class="h-1 w-0 bg-cyan-500 group-hover:w-full transition-all duration-500"
					>
					</div>
				</a>

				<!-- Tarjeta 2 -->
				<a
					href="marketing/index.html"
					class="card-glitch border border-white/10 p-8 bg-black/50 backdrop-blur-sm relative overflow-hidden group"
				>
					<div class="text-magenta-500 mb-6 heading-font text-xl">
						02 // Visual Team
					</div>
					<h3 class="text-2xl font-bold mb-4">Diseño de Ruido</h3>
					<p class="text-gray-400 text-sm leading-relaxed mb-6">
						Identidades visuales que vibran. Estética glitch
						aplicada a marcas que buscan no pasar desapercibidas.
					</p>
					<div
						class="h-1 w-0 bg-magenta-500 group-hover:w-full transition-all duration-500"
					>
					</div>
				</a>

				<!-- Tarjeta 3 -->
				<a
					href="social/index.html"
					class="card-glitch border border-white/10 p-8 bg-black/50 backdrop-blur-sm relative overflow-hidden group"
				>
					<div class="text-yellow-500 mb-6 heading-font text-xl">
						03 // Social Glitch
					</div>
					<h3 class="text-2xl font-bold mb-4">Impacto Social</h3>
					<p class="text-gray-400 text-sm leading-relaxed mb-6">
						El error que el sistema necesitaba.
						<br />Fuera de la pantalla, dentro de la comunidad.
					</p>
					<div
						class="h-1 w-0 bg-yellow-500 group-hover:w-full transition-all duration-500"
					>
					</div>
				</a>
			</div>
		</div>
	</section>

	<Footer />

	<script is:inline>
		setInterval(() => {
			const cards = document.querySelectorAll(".card-glitch");
			const randomCard = cards[Math.floor(Math.random() * cards.length)];
			randomCard.style.opacity = "0.7";
			setTimeout(() => (randomCard.style.opacity = "1"), 50);
		}, 3000);

		const slider = document.getElementById("slider");
		let index = 0;
		const totalSlides = slider.children.length;

		setInterval(() => {
			index = (index + 1) % totalSlides;
			slider.style.transform = `translateX(-${index * 100}%)`;
		}, 4000);

		slider.style.filter = "contrast(200%)";
		setTimeout(() => (slider.style.filter = ""), 100);
	</script>
</Layout>
