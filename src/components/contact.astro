<!-- Sección de Contacto -->
<section id="contact" class="py-44 px-6">
    <div class="max-w-4xl mx-auto text-center">
        <h2 class="heading-font text-4xl mb-8">Establecer Conexión</h2>
        <div class="bg-white/5 p-10 border border-white/10">
            <form id="contact-form" class="grid grid-cols-1 gap-6">
                <div class="text-left">
                    <label
                        class="block text-xs uppercase tracking-widest text-gray-500 mb-2"
                    >
                        Identificador
                    </label>
                    <input
                        type="text"
                        name="user_name"
                        id="user_name"
                        required
                        minlength="2"
                        class="w-full bg-black border border-white/20 p-4 focus:border-cyan-500 outline-none transition-all"
                        placeholder="USER_NAME"
                    />
                </div>

                <div class="text-left">
                    <label
                        class="block text-xs uppercase tracking-widest text-gray-500 mb-2"
                    >
                        Canal de respuesta
                    </label>
                    <input
                        type="email"
                        name="user_email"
                        id="user_email"
                        required
                        class="w-full bg-black border border-white/20 p-4 focus:border-cyan-500 outline-none transition-all"
                        placeholder="user@mail.com"
                    />
                </div>

                <div class="text-left">
                    <label
                        class="block text-xs uppercase tracking-widest text-gray-500 mb-2"
                    >
                        Mensaje
                    </label>
                    <textarea
                        name="message"
                        id="message"
                        rows="4"
                        required
                        minlength="10"
                        class="w-full bg-black border border-white/20 p-4 focus:border-cyan-500 outline-none transition-all"
                        placeholder="Escribe aquí..."></textarea>
                </div>

                <!-- Honeypot (trampa anti-bots invisible) -->
                <input type="text" name="website" id="website" class="hidden" />

                <button
                    type="submit"
                    id="submit-btn"
                    class="btn-glitch w-full py-4 text-sm font-bold uppercase tracking-[0.3em]"
                >
                    Transmitir Datos
                </button>
            </form>
        </div>
    </div>
</section>
